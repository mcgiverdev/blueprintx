# Blueprint base generado por BlueprintX (tenancy)
# Driver detectado: stancl/tenancy
module: tenancy
entity: Tenant
table: tenants
architecture: hexagonal
tenancy:
  mode: central
  storage: central
  routing_scope: central
  seed_scope: central
fields:
  - name: id
    type: uuid
  - name: name
    type: string
    rules: required|string|max:120
  - name: slug
    type: string
    rules: nullable|string|max:120
  - name: domain
    type: string
    rules: nullable|string|max:191
  - name: active
    type: boolean
    default: true
options:
  timestamps: true
  softDeletes: true
relations: []
api:
  base_path: /tenancy/tenants
  middleware:
    - auth:sanctum
    - tenant
  endpoints:
    - type: crud
    - type: search
      fields:
        - name
        - domain
  resources:
    includes: []
docs:
  summary: 'Blueprint base para gestionar tenants'
errors: []
metadata: []
