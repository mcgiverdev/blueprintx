entity: Invoice
module: finance/central
table: invoices
architecture: hexagonal
fields:
  - name: id
    type: uuid
  - name: customer_id
    type: uuid
  - name: total
    type: decimal
    precision: 10
    scale: 2
relations:
  - type: belongsTo
    target: Customer
    field: customer_id
options:
  timestamps: true
  softDeletes: true
api:
  base_path: /finance/invoices
  middleware:
    - auth:sanctum
  endpoints:
    - type: crud
docs:
  description: Facturación centralizada
errors: {}
metadata:
  owner: Finance Ops
  criticality: high
tenancy:
  mode: central
  storage: central
  connection: mysql
  routing_scope: central
  seed_scope: central
